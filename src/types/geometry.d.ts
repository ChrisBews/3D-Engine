type vec3 = {
  x: number;
  y: number;
  z: number;
}

type vec4 = {
  x: number;
  y: number;
  z: number;
  w: number;
}

type point = {
  x: number;
  y: number;
}

type mat3 = [
  number, number, number,
  number, number, number,
  number, number, number
];

type mat4 = [
  number, number, number, number,
  number, number, number, number,
  number, number, number, number,
  number, number, number, number
]
